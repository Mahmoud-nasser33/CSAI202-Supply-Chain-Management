<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Silsila SCM</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <!-- Global Design System -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/loading-states.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/SupplyChain.Frontend.styles.css" asp-append-version="true" />
    <!-- Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <!-- Preload background image for fast loading -->
    <link rel="preload" href="/images/supply_chain_background.png" as="image">
    <!-- Session & Navigation Management -->
    <script src="~/js/session-manager.js"></script>
    <script src="~/js/navigation-manager.js"></script>
    <script src="~/js/notification-manager.js"></script>
    <script src="~/js/mock-data-service.js"></script>
    <script src="~/js/search-manager.js"></script>
    <script src="~/js/loading-manager.js"></script>
    <script src="~/js/error-handler.js"></script>
    <script src="~/js/data-export.js"></script>
    <script src="~/js/modal-manager.js"></script>
    <script src="~/js/table-utilities.js"></script>
    
    <!-- Theme & Animation Loader (Prevents Flash) -->
    <script>
        (function() {
            const theme = localStorage.getItem('theme');
            if(theme === 'dark') {
                document.documentElement.classList.add('dark-mode'); // Apply to HTML tag for earlier parsing
                document.body?.classList.add('dark-mode'); // Fallback
            }
            if(localStorage.getItem('animationPref') !== 'off') {
                document.documentElement.classList.add('breathing-bg');
            }
        })();
    </script>

</head>
<body class="">
    <!-- Page Load Overlay -->

    <div id="wrapper">
        <!-- Sidebar -->
        <div id="sidebar-wrapper">
            <div class="sidebar-heading">
                <i class="bi bi-box-seam-fill text-primary"></i> SILSILA | سلسلة
            </div>
            <div class="list-group list-group-flush mt-3">
                <a asp-page="/Index" data-page="home" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Index" ? "active" : "")">
                    <i class="bi bi-house-door"></i> Home Page
                </a>
                
                <div class="sidebar-label">Operations</div>
                <a asp-page="/Orders" data-page="orders" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Orders" ? "active" : "")">
                    <i class="bi bi-cart"></i> Orders
                </a>
                <a asp-page="/Inventory" data-page="inventory" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Inventory" ? "active" : "")">
                    <i class="bi bi-boxes"></i> Inventory
                </a>
                <a asp-page="/Shipments" data-page="shipments" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Shipments" ? "active" : "")">
                    <i class="bi bi-truck"></i> Shipments
                </a>
                <a asp-page="/Products" data-page="products" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Products" ? "active" : "")">
                    <i class="bi bi-tag"></i> Products
                </a>

                <div class="sidebar-label">Management</div>
                <a asp-page="/Customers" data-page="customers" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Customers" ? "active" : "")">
                    <i class="bi bi-people"></i> Customers
                </a>
                <a asp-page="/Suppliers" data-page="suppliers" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Suppliers" ? "active" : "")">
                    <i class="bi bi-building"></i> Suppliers
                </a>
                <a asp-page="/Warehouses" data-page="warehouses" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Warehouses" ? "active" : "")">
                    <i class="bi bi-geo-alt"></i> Warehouses
                </a>
                 <a asp-page="/Payments" data-page="payments" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Payments" ? "active" : "")">
                    <i class="bi bi-credit-card"></i> Payments
                </a>

                <div class="sidebar-label">Insights</div>
                <a asp-page="/Analytics" data-page="analytics" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Analytics" ? "active" : "")">
                    <i class="bi bi-graph-up"></i> Analytics
                </a>
                <a asp-page="/Feedback" data-page="feedback" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Feedback" ? "active" : "")">
                    <i class="bi bi-chat-quote"></i> Feedback
                </a>
                
                <div class="sidebar-label">System</div>
                <a asp-page="/Users" data-page="users" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Users" ? "active" : "")">
                    <i class="bi bi-person-gear"></i> Users
                </a>
                <a asp-page="/Profile" data-page="profile" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Profile" ? "active" : "")">
                    <i class="bi bi-person-circle"></i> Profile
                </a>
                <a asp-page="/Settings" data-page="settings" class="list-group-item list-group-item-action @(ViewContext.RouteData.Values["page"]?.ToString() == "/Settings" ? "active" : "")">
                    <i class="bi bi-sliders"></i> Settings
                </a>
            </div>
        </div>
        <!-- /#sidebar-wrapper -->

        <!-- Page Content -->
        <div id="page-content-wrapper">
            <!-- Top Navigation -->
            <nav class="top-navbar justify-content-between">
                <div>
                     <span class="text-muted fw-bold">@ViewData["Title"]</span>
                </div>
                
                <div class="d-flex align-items-center gap-3">
                    <!-- Search Mock -->
                    <div class="input-group input-group-sm d-none d-md-flex" style="width: 250px;">
                        <input type="text" class="form-control bg-light border-0" placeholder="Search..." aria-label="Search">
                        <span class="input-group-text bg-light border-0"><i class="bi bi-search"></i></span>
                    </div>

                    <!-- Actions -->
                    <button class="btn btn-outline-secondary d-md-none me-2" id="sidebarToggle">
                        <i class="bi bi-list"></i>
                    </button>

                    <a asp-page="/Notifications" class="text-secondary position-relative me-2">
                        <i class="bi bi-bell fs-5"></i>
                        <span class="position-absolute top-0 start-100 translate-middle p-1 bg-danger border border-light rounded-circle">
                            <span class="visually-hidden">New alerts</span>
                        </span>
                    </a>
                    
                    <div class="dropdown">
                        <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="https://ui-avatars.com/api/?name=Admin+User&background=10b981&color=fff" alt="" width="32" height="32" class="rounded-circle me-2">
                            <span class="d-none d-md-inline fw-bold text-dark small">Admin User</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0" aria-labelledby="userDropdown">
                             <li><a class="dropdown-item" asp-page="/Profile">Profile</a></li>
                            <li><a class="dropdown-item" asp-page="/Settings">Settings</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" asp-page="/Login">Logout</a></li>
                        </ul>
                    </div>
                </div>
            </nav>

            <div id="content-scrollable">
                <!-- Main Content Container -->
                <div class="container-fluid p-4">
                    @RenderBody()
                </div>
                
                 <footer class="mt-auto py-3 text-center text-muted small border-top bg-white">
                    &copy; 2025 Silsila Supply Chain Management System
                </footer>
            </div>
        </div>
        <!-- /#page-content-wrapper -->
        
    </div>
    <!-- /#wrapper -->

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <!-- Draggable Truck Widget -->
    <div id="clickableHub" title="I deliver happiness!">
        🚚
    </div>

    <!-- Draggable Widget Script -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Sidebar Toggle Logic
            const sidebarToggle = document.getElementById('sidebarToggle');
            if (sidebarToggle) {
                sidebarToggle.addEventListener('click', function(e) {
                    e.preventDefault();
                    document.body.classList.toggle('sidebar-toggled');
                });
            }
            
            // Close sidebar when clicking overlay (mobile)
            document.addEventListener('click', function(e) {
                if (window.innerWidth < 768 && 
                    document.body.classList.contains('sidebar-toggled') && 
                    !e.target.closest('#sidebar-wrapper') && 
                    !e.target.closest('#sidebarToggle')) {
                    document.body.classList.remove('sidebar-toggled');
                }
            });

            // Apply breathing class initially...
            if(localStorage.getItem('animationPref') !== 'off') {
                document.body.classList.add('breathing-bg');
            }

            const truck = document.getElementById('clickableHub');
            if(truck) {
                let isDragging = false;
                let startX, startY, initialLeft, initialTop;

                truck.addEventListener('mousedown', (e) => {
                    isDragging = true;
                    startX = e.clientX;
                    startY = e.clientY;
                    
                    const rect = truck.getBoundingClientRect();
                    initialLeft = rect.left;
                    initialTop = rect.top;

                    truck.style.bottom = 'auto';
                    truck.style.right = 'auto';
                    truck.style.left = initialLeft + 'px';
                    truck.style.top = initialTop + 'px';
                    truck.style.cursor = 'grabbing';
                });

                document.addEventListener('mousemove', (e) => {
                    if (!isDragging) return;
                    e.preventDefault();
                    const dx = e.clientX - startX;
                    const dy = e.clientY - startY;
                    truck.style.left = (initialLeft + dx) + 'px';
                    truck.style.top = (initialTop + dy) + 'px';
                });

                document.addEventListener('mouseup', () => {
                    if(isDragging) {
                        isDragging = false;
                        truck.style.cursor = 'grab';
                        truck.animate([
                            { transform: 'scale(1)' },
                            { transform: 'scale(1.2)' },
                            { transform: 'scale(1)' }
                        ], { duration: 300 });
                    }
                });
            }
        });

    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>