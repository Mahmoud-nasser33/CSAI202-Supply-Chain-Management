@page
@model SupplyChain.Frontend.Pages.ProfileModel
@{
    ViewData["Title"] = "User Profile";
}

<div class="container-bento py-4">
    <div class="d-flex justify-content-between align-items-center mb-4 animate-enter">
        <div>
            <h2 class="fw-bold mb-0">My Profile</h2>
            <p class="text-muted mb-0">Personal information and security</p>
        </div>
    </div>

    <div class="bento-grid">
        <!-- Identity Card (Span 2) -->
        <div class="card border-0 glass-panel shadow-sm span-col-2 animate-enter delay-1 h-100 bg-gradient-primary text-white position-relative overflow-hidden" style="background: linear-gradient(135deg, #0f172a 0%, #334155 100%) !important;">
             <div class="card-body text-center p-5 d-flex flex-column align-items-center justify-content-center">
                 <div class="position-relative mb-4">
                    <div class="bg-white p-1 rounded-circle shadow-lg" style="width: 140px; height: 140px;">
                        <img id="avatarPreview" src="https://ui-avatars.com/api/?name=Admin+User&background=6366f1&color=fff&size=512" class="w-100 h-100 rounded-circle object-fit-cover" alt="Profile">
                    </div>
                    <label for="avatarUpload" class="btn btn-light rounded-circle position-absolute bottom-0 end-0 shadow-sm border" title="Change Photo" style="width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; cursor: pointer;">
                        <i class="bi bi-camera-fill text-dark"></i>
                        <input type="file" id="avatarUpload" icon="image/*" class="d-none">
                    </label>
                </div>
                <h3 class="fw-bold mb-1" id="displayName">Admin User</h3>
                <p class="text-light text-opacity-75 mb-3" id="displayRole">Administrator • Cairo Branch</p>
                <div class="d-flex gap-2">
                     <span class="badge bg-white bg-opacity-25 border border-white border-opacity-25 px-3 py-2 rounded-pill">Active</span>
                     <span class="badge bg-white bg-opacity-25 border border-white border-opacity-25 px-3 py-2 rounded-pill">Verified</span>
                </div>
             </div>
        </div>

        <!-- Edit Form Card (Span 2) -->
        <div class="card border-0 glass-panel shadow-sm span-col-2 animate-enter delay-2">
            <div class="card-body p-4">
                <h5 class="fw-bold mb-4">Account Details</h5>
                <form method="post" id="profileForm">
                    <div class="row g-4">
                        <div class="col-md-6">
                            <label class="form-label fw-bold small text-muted text-uppercase">Full Name</label>
                            <div class="input-group shadow-sm">
                                <span class="input-group-text bg-light border-0"><i class="bi bi-person"></i></span>
                                <input type="text" class="form-control border-0 bg-light fw-bold" id="FullName" name="FullName" value="Admin User">
                            </div>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label fw-bold small text-muted text-uppercase">Email Address</label>
                            <div class="input-group shadow-sm">
                                <span class="input-group-text bg-light border-0"><i class="bi bi-envelope"></i></span>
                                <input type="email" class="form-control border-0 bg-light fw-bold" id="Email" name="Email" value="admin@silsila.com">
                            </div>
                        </div>
                         <div class="col-md-6">
                            <label class="form-label fw-bold small text-muted text-uppercase">Phone Number</label>
                            <div class="input-group shadow-sm">
                                <span class="input-group-text bg-light border-0"><i class="bi bi-telephone"></i></span>
                                <input type="tel" class="form-control border-0 bg-light fw-bold" id="Phone" name="Phone" value="+20 123 456 7890">
                            </div>
                        </div>
                         <div class="col-md-6">
                            <label class="form-label fw-bold small text-muted text-uppercase">Role</label>
                            <input type="text" class="form-control border-0 bg-light" value="System Administrator" disabled readonly>
                        </div>
                        
                        <div class="col-12 mt-4 d-flex justify-content-end gap-2">
                            <button type="button" class="btn btn-outline-secondary px-4 fw-bold">Cancel</button>
                            <button type="submit" class="btn btn-primary px-4 fw-bold shadow-sm"><i class="bi bi-save me-2"></i> Save Changes</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Load saved data
        if(localStorage.getItem('profile_fullname')) {
            document.getElementById('FullName').value = localStorage.getItem('profile_fullname');
            document.getElementById('displayName').innerText = localStorage.getItem('profile_fullname');
        }
        if(localStorage.getItem('profile_email')) document.getElementById('Email').value = localStorage.getItem('profile_email');
        if(localStorage.getItem('profile_phone')) document.getElementById('Phone').value = localStorage.getItem('profile_phone');
        if(localStorage.getItem('profile_avatar')) document.getElementById('avatarPreview').src = localStorage.getItem('profile_avatar');

        // Photo Upload Preview
        const avatarInput = document.getElementById('avatarUpload');
        const avatarPreview = document.getElementById('avatarPreview');

        avatarInput.addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    avatarPreview.src = e.target.result;
                    localStorage.setItem('profile_avatar', e.target.result); 
                }
                reader.readAsDataURL(file);
            }
        });

        // Save Logic
        document.getElementById('profileForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Save to LocalStorage
            const name = document.getElementById('FullName').value;
            localStorage.setItem('profile_fullname', name);
            localStorage.setItem('profile_email', document.getElementById('Email').value);
            localStorage.setItem('profile_phone', document.getElementById('Phone').value);
            
            document.getElementById('displayName').innerText = name;

            // UI Feedback
            const btn = this.querySelector('button[type="submit"]');
            const originalText = btn.innerHTML;
            btn.innerHTML = '<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Saving...';
            btn.disabled = true;

            setTimeout(() => {
                btn.innerHTML = '<i class="bi bi-check-lg"></i> Saved!';
                btn.classList.replace('btn-primary', 'btn-success');
                setTimeout(() => {
                    btn.innerHTML = originalText;
                    btn.classList.replace('btn-success', 'btn-primary');
                    btn.disabled = false;
                }, 2000);
            }, 800);
        });
    });
</script>
